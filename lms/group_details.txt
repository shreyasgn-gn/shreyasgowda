<?php
if (!isset($_GET['group_id'])) {
    die("Group ID is missing.");
}

$group_id = intval($_GET['group_id']);
$conn = new mysqli("localhost", "root", "", "worker");
if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

// Get group details
$group_sql = "SELECT * FROM worker_groups WHERE id = $group_id";
$group_result = $conn->query($group_sql);
$group = $group_result->fetch_assoc();

// Get group members
$members_sql = "SELECT * FROM group_members WHERE group_id = $group_id";
$members_result = $conn->query($members_sql);
?>

<!DOCTYPE html>
<html>
<head>
    <title><?php echo $group['group_name']; ?> - Members</title>
    <link rel="stylesheet" href="styleshomes.css">
    <style>
        .group-info {
            text-align: center;
            margin: 30px;
        }

        .group-info h2 {
            margin-bottom: 10px;
        }

        .members-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
        }

        .member-card {
            background: #fff;
            border: 1px solid #ccc;
            border-radius: 10px;
            margin: 15px;
            padding: 20px;
            width: 250px;
            text-align: center;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }

        .member-card img {
            max-width: 100%;
            height: 180px;
            object-fit: cover;
            border-radius: 8px;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <div class="group-info">
        <h2><?php echo $group['group_name']; ?></h2>
        <p><strong>Address:</strong> <?php echo $group['address']; ?></p>
        <p><strong>Phone:</strong> <?php echo $group['phone']; ?></p>
    </div>

    <div class="members-container">
        <?php while ($member = $members_result->fetch_assoc()) { ?>
            <div class="member-card">
                <img src="uploads/<?php echo $member['photo']; ?>" alt="Member Photo">
                <h3><?php echo $member['name']; ?></h3>
                <p><strong>Experience:</strong> <?php echo $member['experience']; ?> years</p>
            </div>
        <?php } ?>
    </div>
</body>
</html>

<?php $conn->close(); ?>
